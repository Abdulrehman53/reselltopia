class ProductModel {
  String name, imageLocation, oldPrice, price, description, condition, id, type;
  bool isFav, isSaved;
  ProductModel({this.name, this.imageLocation, this.oldPrice, this.price});
}
